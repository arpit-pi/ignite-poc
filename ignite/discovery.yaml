apiVersion: v1
kind: Service
metadata:
  name: ignite-discovery-service
  namespace: ignite
  labels:
    app: ignite
spec:
  clusterIP: None
  ports:
    - name: discovery
      port: 47500
  selector:
      app: ignite
